/**
 * Re-export AuthProvider and useAuth from ../contexts/AuthContext
 * Ensures compatibility across single / plural context directory references.
 */
export { AuthProvider, useAuth } from '../contexts/AuthContext';
